# SPDX-License-Identifier: AGPL-3.0-or-later
"""asalloc command line interface.

Entry module: :mod:`cli.cli` (console script ``cli.cli:main``).
Import paths are explicit (``cli.cli``, ``cli.update``, ...) and this
package root intentionally defines nothing: leaf submodules such as
:mod:`cli.common` must stay importable while the ``portfolio`` package
is still initializing.
"""
